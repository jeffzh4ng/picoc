picoc089 is a C0 interpreter and C89->RV32I compiler

Usage:
cargo run interpretc0 ./path/to/file.c0
cargo run compilec89 ./path/to/file.c

Correct:
snap sublangs (arith, bindings, control, heap)
smokes with egos2000, xv6, git, and sqlite
diffed against c0deine and gcc

Speed:
20% opts for 80% perf
inline, unroll, cse, dce, motion, fold, peephole